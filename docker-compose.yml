version: "3.8"

services:
  resume-server:
    build: .
    container_name: resume-server
    ports:
      - "8313:8000"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      # 如需持久化上传的文件或日志可挂载目录
      - ./data:/app/data
